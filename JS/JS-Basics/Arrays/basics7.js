let lists = [1, 2, 3, 4];
let numbers = [5,6 , 7, 8];

let result = lists.join(',');
result = result.split(',');
console.log(result);
// lists.length = 0;

// lists = [];

// while (lists.length > 0)
//     lists.pop();

// lists.splice(0);

// const result = lists.concat(numbers);

// const result = [...lists, ...numbers, 9, 10];


